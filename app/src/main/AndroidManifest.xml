<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aira_fun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Airafun">

        <!-- LOGIN JADI HALAMAN AWAL -->
        <activity
            android:name="com.example.aira_fun.pertemuan_3.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- WELCOME -->
        <activity
            android:name="com.example.aira_fun.pertemuan_3.WelcomeActivity"
            android:exported="false" />

        <!-- MAIN -->
        <activity
            android:name="com.example.aira_fun.MainActivity"
            android:exported="false" />

    </application>

</manifest>